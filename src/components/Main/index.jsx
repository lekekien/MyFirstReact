import React from 'react';
import {Link} from "react-router-dom";
const Main = props => {
    return (
        <div>
           <h1>main</h1>
        </div>
    );
};

Main.propTypes = {
    
};

export default Main;